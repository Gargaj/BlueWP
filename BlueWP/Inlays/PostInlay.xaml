<UserControl
    x:Class="BlueWP.Inlays.PostInlay"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:BlueWP.Inlays"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="200"/>
      <RowDefinition Height="35"/>
    </Grid.RowDefinitions>
    <Grid.ColumnDefinitions>
    </Grid.ColumnDefinitions>
    <TextBox Grid.Row="0" Grid.ColumnSpan="2" Text="{Binding PostText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Height="200" BorderThickness="1,1,1,1" BorderBrush="Black" AcceptsReturn="True"/>
    <TextBlock Grid.Row="1" Grid.Column="0" Text="{Binding PostLengthText}" Margin="5,7,0,0"/>
    <Button Grid.Row="1" Grid.Column="1" HorizontalAlignment="Right" Click="Send_Click">
      <FontIcon Glyph="&#xE724;"/>
    </Button>
  </Grid>
</UserControl>
